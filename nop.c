#include "main.h"
/**
 * nop - Function don't do anything.
 * @head: The top of the stack.
 * @lineno: The line number in the file.
 * Return: Nothing.
 */
void nop(stack_t **head, int lineno)
{
	(void)head;
	(void)lineno;
}
